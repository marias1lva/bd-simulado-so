//por enquanto é um placeholder — vai ser implementado na parte final com comunicação real via pipe ou memória compartilhada
//para compilar: g++ -std=c++17 cliente.cpp -o cliente
//para executar: ./cliente (após o servidor estar rodando)

#include <iostream>

int main() {
    std::cout << "[CLIENTE] Placeholder — IPC via pipe sera implementado na entrega final.\n";
    std::cout << "[CLIENTE] Por enquanto, o servidor.cpp enfileira as requisicoes internamente.\n";
    return 0;
}